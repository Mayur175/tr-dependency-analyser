#!/usr/bin/env bash
# Hook: PreDeploy
#
# Scaffold placeholder. Created from the project template tree on 2026-06-20.
# This script is a no-op stub. Implement the real hook body before
# wiring it into .claude/settings.json — Claude Code will execute it
# verbatim, so do not register it until the body is real.
#
# See https://docs.claude.com/en/docs/claude-code/hooks for the expected
# hook contract (stdin payload shape, exit codes, environment variables).

echo "[hook PreDeploy] scaffold stub — no-op" >&2
exit 0
