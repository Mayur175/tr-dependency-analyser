{
  "$schema": "https://json.schemastore.org/claude-code-settings.json",
  "$comment": "Project-scoped Claude Code settings. Personal/per-developer overrides go in .claude/settings.local.json (gitignored).",
  "permissions": {
    "allow": [],
    "deny": []
  },
  "hooks": {
    "$comment": "Hook scaffolds in .claude/hooks/ are not yet wired. Register them here only after the script bodies are real."
  }
}
