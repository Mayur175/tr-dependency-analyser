# Icons

Place a 16x16 PNG icon here named `dependency.png`.

Referenced from `plugin.xml` for the context menu, top-level menu,
main toolbar button, and view tab.

If the file is absent, Eclipse falls back to a default missing-icon
placeholder; the plugin still functions correctly.